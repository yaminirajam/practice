#include <stdio.h>

int main()
{
    int i=5,arr1[i],arr2[i],sumarr[i],count=0;
    for(i=0;i<5;i++)
        scanf("%d",&arr1[i]);
    for(i=0;i<5;i++)
    scanf("%d",&arr2[i]);
    for(i=0;i<5;i++){
    sumarr[i]=arr1[i]+arr2[i];
    count+=sumarr[i];};
    printf("%d",count);
    return 0;
}
