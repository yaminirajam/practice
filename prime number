#include<stdio.h>
int main()
{
    int a,count=0;
    scanf("%d",&a);
    for(int i=1;i<=10;i++)
    {
        if(a%i==0)
        count++;
    }
    if(count==2)
        printf("It is a prime number");
    else
        printf("Its not a prime number");
    return 0;
}
